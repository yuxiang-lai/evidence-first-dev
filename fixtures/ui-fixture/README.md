# UI Fixture

This page is the existing visual language for a UI change: CSS variables define
color, spacing, and radius, while the settings panel demonstrates labels,
responsive behavior, and a status message. A prototype should reuse these
references rather than introduce a second design system.
