# Resume Fixture

This repository starts with stable project memory and a runnable command. The
fixture smoke test initializes a Fast change, simulates a session boundary, and
uses the generated workflow entry to recover the current task.
